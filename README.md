# Virtusa_PasswordValidator
A Core Java-based password validation tool that enforces security rules like minimum length, uppercase letters, and digits. It provides clear feedback for invalid inputs and keeps prompting the user until a strong password is entered.
